# TODO

- [ ] Add edit support to DetailModal (generic editable fields, excluding `id`, JSON/string handling)
- [ ] Add “Edit” button in each module list next to “Delete”
- [ ] Wire Edit action to DetailModal with correct `moduleId`, `item`, and `onUpdate` handler using `DataManagers[moduleId].update`
- [ ] Refresh module list + selected item after save
- [ ] Run build/lint (npm test/build) to ensure no runtime/compile errors

